//Remove the index table
$(".indexTable").remove()

// Make sure all inputs are set to 0
$("#product_valor_maximo_cop").val(0)
$("#product_valor_usd").val(0)
$("#product_peso").val(0)
$("#product_name").val(" ")

//Remove any existing the new form card 
$(".cardInitial").remove()

// Add New form from Index 
$(".newFormFromIndex").append('<%= j render("form") %>')   //.show()

//Show the new form card 
$(".cardInitial").show()


